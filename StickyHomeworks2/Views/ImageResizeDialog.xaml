<Window x:Class="StickyHomeworks.Views.ImageResizeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:ely="http://github.com/HelloWRC/ElysiaFramework"
        mc:Ignorable="d"
        FontFamily="{StaticResource HarmonyOsSans}"
        Title="调整图片大小"
        Width="400"
        Height="220"
        MinWidth="350"
        MinHeight="200"
        Background="{x:Null}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30" ResizeBorderThickness="4"/>
    </WindowChrome.WindowChrome>

    <materialDesign:Card Margin="6"
                         Background="{DynamicResource MaterialDesignPaper}">
        <Grid Margin="0">
            <StackPanel VerticalAlignment="Center">
            <!-- 标题栏 -->
            <Grid Margin="0,0,0,15">
                <TextBlock Text="调整图片大小" 
                           FontSize="20" 
                           Foreground="{DynamicResource MaterialDesignBody}"
                           HorizontalAlignment="Center"/>
            </Grid>

            <!-- 百分比显示 -->
            <TextBlock x:Name="ZoomPercentText"
                       Text="100%"
                       FontSize="28"
                       Foreground="{DynamicResource MaterialDesignBody}"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,20"/>

            <!-- 滑条 -->
            <Slider x:Name="ZoomSlider"
                    Width="300"
                    Minimum="10"
                    Maximum="300"
                    Value="100"
                    TickFrequency="10"
                    TickPlacement="None"
                    IsSnapToTickEnabled="True"
                    AutoToolTipPlacement="BottomRight"
                    AutoToolTipPrecision="0"
                    ValueChanged="ZoomSlider_ValueChanged"
                    Margin="0,0,0,20"/>

            <!-- 按钮区域 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                        Margin="0 0 12 0"
                        Click="OkButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Check" Margin="0 0 6 0"/>
                        <TextBlock Text="确定"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                        Click="CancelButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Close" Margin="0 0 6 0"/>
                        <TextBlock Text="取消"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>
